# Generic boilerplate product-leak allowlist.
#
# Keep this list as narrow as possible: every entry is an exact tracked path
# exempted from the product-term scan in .factory/tools/check-generic-leakage.sh.
# Prefer neutralizing product content over allowlisting it. An adopting
# product must instead delete this allowlist when it replaces the boilerplate.
#
# The canonical migration specification names the source product solely to
# state that it is not this workspace and must not be built here. It is
# immutable during implementation and is the sole exact exception.
docs/FACTORY-LOOP-SPEC.md
.factory/loop/redaction.py
.factory/tests/test-factory-redaction.py

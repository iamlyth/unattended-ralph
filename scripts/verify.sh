#!/usr/bin/env bash
# verify.sh — project verification entry point (boilerplate placeholder).
#
# Replace this with your project's actual build + test command.
# The factory harness calls this as the per-runner verify_command and as
# the overall verification.command from .factory/config.toml.
set -euo pipefail
echo "verify.sh: boilerplate placeholder — replace with your project's build + test command"
exit 0